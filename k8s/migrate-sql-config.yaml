apiVersion: v1
kind: ConfigMap
metadata:
  name: migrate-sql-config
data:
  migrate.sql: |
    -- Example migration: create users table if not exists
    CREATE TABLE IF NOT EXISTS users (
      id SERIAL PRIMARY KEY,
      name TEXT NOT NULL,
      email TEXT UNIQUE NOT NULL,
      created_at TIMESTAMP NOT NULL DEFAULT NOW()
    );
