name: Full CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  ci-cd:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Setup Minikube
        uses: medyagh/setup-minikube@v0.0.14
        with:
          kubernetes-version: v1.30.0

      - name: Apply Kubernetes manifests
        run: |
          kubectl apply -f k8s/postgres-config.yaml
          kubectl apply -f k8s/postgres-secret.yaml
          kubectl apply -f k8s/postgres-deployment.yaml
          kubectl apply -f k8s/postgres-service.yaml
          kubectl rollout status deployment/postgres-deployment

      - name: Reset database
        run: |
          kubectl apply -f k8s/reset-sql-config.yaml
          kubectl apply -f k8s/db-reset-job.yaml
          kubectl wait --for=condition=complete job/db-reset-job --timeout=60s

      - name: Run DB migrations
        run: |
          kubectl apply -f k8s/migrate-sql-config.yaml
          kubectl apply -f k8s/db-migrate-job.yaml
          kubectl wait --for=condition=complete job/db-migrate-job --timeout=60s

      - name: Run tests
        run: npm test
        env:
          POSTGRES_HOST: postgres-service
          POSTGRES_PORT: 5432
          POSTGRES_USER: miniapiuser
          POSTGRES_PASSWORD: miniapipass
          POSTGRES_DB: miniapidb

      - name: Build Docker image
        run: |
          docker build -t mini-api-ts:latest .
          minikube image load mini-api-ts:latest

      - name: Deploy API to Kubernetes
        run: |
          kubectl apply -f k8s/api-deployment.yaml
          kubectl apply -f k8s/api-service.yaml
          kubectl rollout status deployment/api-deployment
